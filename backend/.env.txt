# Security settings
SECRET_KEY=your-secure-secret-key-for-development
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database configuration
DB_NAME=evodoc
DB_USER=evodoc_user
DB_PASSWORD=raunak718
DB_HOST=localhost
DB_PORT=5432

# Optional: Use SQLite instead of PostgreSQL for development
# USE_SQLITE=True
